BEGIN rqjah4

IF ~Global("rqharper3reviane","GLOBAL",1)
    ~
    THEN BEGIN JaheiraUnnatural
    SAY @0
    IF ~~ THEN REPLY @1 GOTO Jaheira4Ressurected
END

IF ~~ THEN BEGIN Jaheira4Ressurected
    SAY @2
    IF ~~ THEN REPLY @3 EXTERN ~rqdermin~ DerminWhatYouDone
    IF ~~ THEN REPLY @4 EXTERN ~rqdermin~ DerminWhatYouDone
    IF ~~ THEN REPLY @5 EXTERN ~rqdermin~ DerminWhatYouDone
END




